# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# Environment / secrets
.env
.streamlit/secrets.toml

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Build / logs
*.log

# Tooling: Claude Code per-project settings + notebook checkpoints
.claude/
.ipynb_checkpoints/

# Bootstrap doc — kept on disk for reference but untracked once the app
# is past first-deploy (it's the template's setup checklist, not part of
# the app itself).
SETUP.md
